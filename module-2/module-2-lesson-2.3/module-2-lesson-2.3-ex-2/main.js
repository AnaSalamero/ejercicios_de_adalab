"use strict";

let person = 'Sara';

if (person === 'Ana' ) {
    console.log(`Bienvenida, ${person}.`)
} else {
    console.log ('Lo siento pero el usuario que has introducido no esta registrado')
};